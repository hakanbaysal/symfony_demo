{% extends '::blog.html.twig' %}

{% block body -%}
<div class="container">
    <h1>Post edit</h1>

    {{ form(edit_form, { 'attr': {'class': 'form-inline'}}) }}

    {{ form(delete_form) }}

	 <a href="{{ path('post') }}" class="btn btn-primary">
            Back to the list
        </a>

<br /><br />
</div>
{% endblock %}
